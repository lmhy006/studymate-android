# 更新日志

本项目的发布节奏与「应用内检查更新」绑定：每个版本以 `vX.Y.Z` 标签发布到
[GitHub Releases](https://github.com/lmhy006/studymate-android/releases)，并附带 APK 资产。
发布步骤见 [docs/RELEASE.md](docs/RELEASE.md)。

## [1.2.3] - 2026-09-24

### 修复
- **周视图左栏“结束时间被裁切一半”**：40dp 行高内堆了三行 M3 默认行高（16sp）的文字导致第三行被裁；改为紧凑行高（8/9sp 字号 + 9/10sp 行高），三行完整显示；
- **桌面小组件倒数日天数不自动更新**：定位为每日刷新任务播种为“启动后 1 小时 + 每 24h”——不锚定零点，且 iQOO/vivo 省电策略会推迟后台周期任务，导致跨天后仍是旧天数；现改为每日任务**锚定次日 00:10 播种**（每次打开 App 自动重置播种，刷新时按当天实时重算天数），并补充 `MY_PACKAGE_REPLACED`（应用更新后自动重新排期）。若厂商省电策略仍拦截后台任务，可在系统设置中允许拾光“自启动 / 忽略电池优化”。

### 验证
- JVM 单元测试 76 例全部通过；`assembleDebug` / `assembleDebugAndroidTest` 通过；`lintDebug` 0 错误 0 警告。

## [1.2.2] - 2026-09-24

### 新增 / 优化（日程）
- **周视图显示优化**：课程名显示完整（按块高自动换行）；左列节次栏窄化，时间拆两行（上课 `08:00` / `-08:45`，`-` 顶出）；有地点时在课程序块内显示地点；行高压缩至 40dp，13 节课尽量一屏放下；
- **当前时间横线**：周视图各列绘制当前时间**黑色**横线（仿 BIT101，黑色不刺眼）；
- **倒数日联动**：周视图表头与月视图日期用第三强调色高亮“倒数日当天”（与今日主色区分），设置 → 日程设置 → 「高亮倒数日当天」可开关（默认开）；
- **快捷切周**：仿 BIT101，FAB 上方叠放 左箭头（上一周）/ 右箭头（下一周），与头部箭头等效。

### 验证
- JVM 单元测试 76 例全部通过；`assembleDebug` / `assembleDebugAndroidTest` 通过；`lintDebug` 0 错误 0 警告。

## [1.2.1] - 2026-09-24

### 修复
- **逐课速录节次滑杆“第8节变第7节”（恶性）**：滑杆离散值经浮点运算会得到 7.99999…，原 `.toInt()` 截断成 7；改为 `roundToInt()`，第 8、9、12 等节次 tick 全部恢复正常；
- **课程配色**：日程颜色改为**按课程名稳定哈希**分配——同一门课（同名）多次添加/多条记录颜色一致；颜色盘由 8 色扩到 **12 色**，降低“不同课程撞色”概率；新建周期日程颜色自动跟随标题（手动选色后不再自动变）。

### 验证
- JVM 单元测试 76 例全部通过（新增 课程配色 3 例：同名同色/哈希稳定在色盘内/常见课名分散度）；`assembleDebug` / `assembleDebugAndroidTest` 通过；`lintDebug` 0 错误 0 警告。

## [1.2.0] - 2026-09-24

### 新增：单双周（教学周奇偶）
- **导入课程支持单双周**：BIT101 JSON 新增可选字段 `week_parity`（`odd`/`even`/`single`/`double`/`单周`/`双周`），逐课速录新增「每周 / 单周 / 双周」选择；
- **周期日程模型支持单双周**（`schedules.weekParity`，Room v4→v5 迁移）：周/月视图按教学周奇偶自动展开——第 k 周以“规则起点所在周的周一”为第 1 周计算（与教学周起点对齐一致）；
- **手动周期日程可设单双周**（日程编辑弹层）；日程详情显示“单周/双周”标识；导入备注自动带“单周/双周”。

### 验证
- JVM 单元测试 73 例全部通过（新增 单双周 5 例：奇偶匹配/教学周序号/物化过滤、导入解析与转换 2 例）；`assembleDebug` / `assembleDebugAndroidTest` 通过；`lintDebug` 0 错误 0 警告。

## [1.1.2] - 2026-09-24

### 新增
- **时间表编辑器增强（设置 → 日程设置 → 时间表）**：
  - **自定义每日节数**（默认 13 节，1..20），加减即时生效；
  - **每节固定时长（可选）**：开启后输入每节“开始时间”自动计算“下课时间”（= 开始 + 时长）；
  - 仍支持逐节手动指定上课/下课时间、恢复默认。
- **联动适配自定义节数**：节次周视图按当前节数绘制；逐课速录的节次滑杆范围跟随当前节数；BIT101 JSON 导入解析上限放宽到 20 节，超出当前节数的课程在转换时自动跳过。
- **校验错误提示（修复）**：时间表编辑时若出现时间冲突 / 结束早于开始 / 时长未填，错误提示改为**置顶醒目显示**（此前位于列表底部不可见，表现为“点了保存没反应”）。

### 验证
- JVM 单元测试 68 例全部通过（新增 超出节数课程跳过 1 例）；`assembleDebug` / `assembleDebugAndroidTest` 通过；`lintDebug` 0 错误 1 警告（已定位，不影响功能）。

## [1.1.1] - 2026-09-24

### 新增（对应发布说明）
- **教学周起点设置（重要修复）**：新增「本学期教学周起点（第1周）」设置（设置 → 日程设置），逐课速录与 BIT101 JSON 导入都以它为基准对齐教学周——第 k 周 = 起点周周一 + 7×(k−1) + 星期偏移，**修复此前“起始周从本周/下周算、与真实教学周错位”的问题**；导入弹层内也可改并自动保存为默认；
- **自定义时间表**：设置 → 日程设置 → 时间表，逐节编辑每节课“上课/下课时间”（仿 BIT101 设置-课程表设置-时间表），保存后节次周视图与课表导入即时按新时间表换算；支持“恢复默认”（08:00 起 13 节）。

### 验证
- JVM 单元测试 67 例全部通过（新增 时间表序列化/解析/自定义参与计算 3 例）；`assembleDebug` / `assembleDebugAndroidTest` 通过；`lintDebug` 0 错误 0 警告。

## [1.1.0] - 2026-09-24

### 新增：DDL（课程作业截止）
- 位于 **日程 Tab 内**（周视图 / 月视图 / DDL 三个视图切换）；
- 添加 DDL：作业内容、**关联课程**（从日程中已有课程选择，可跳过）、**提交截止时间**（日期+时刻）、提交方式（可选）、备注（可选）；
- **勾选“已完成”**，完成 N 天后自动释放（N 可配置，设置 → 日程设置，默认 3 天）；
- **与日程联动**：点击周/月视图里的课程 → 详情除课程信息外显示该课**关联 DDL**（未完成红字标过期），并可一键「为该课程添加 DDL」；DDL 视图顶部横幅提醒（今日截止 / 已过期）；
- 截止提醒为 App 内提示（沿用无系统通知的设定）；Room v3→v4 迁移（已有数据无损升级）。

### 验证
- JVM 单元测试 63 例全部通过（新增 DDL 截止/释放计算 3 例）；`assembleDebug` / `assembleDebugAndroidTest` 通过；`lintDebug` 0 错误 0 警告。

## [1.0.3] - 2026-09-24

> 说明：v1.0.2 的 Release 误挂了内部版本仍为 1.0.2 的 APK（“关于”显示的版本号取自 APK 内 `versionName`，与 Release tag 无关），
> 本版本修正：**versionCode 4 / versionName 1.0.3**，功能集与 1.0.2 相同
> （Gitee 默认更新源 + GitHub 备用、逐课速录、打卡模式选择优化、关于去掉 versionCode 括号）。

## [1.0.2] - 2026-09-24

### 新增
- **应用内更新双源自动切换**：GitHub 优先，失败自动切到 Gitee（码云）备用源，下载 APK 失败也会回退到 Gitee（国内直连友好；需把仓库镜像到 Gitee 并同步发布同名 Release）；
- **逐课速录课表**：表单式录入 课程名/星期/起止节次/教学周/教师/教室，可连续添加多门后生成导入（与 JSON 导入同构，免手写 JSON）；
- **打卡模式选择优化**：仅在选择「每 N 天」时才显示 周期/自由 模式选择；每日、每周固定为周期模式。

### 验证
- JVM 单元测试 60 例全部通过（新增双源合并策略 6 例）；`assembleDebug` / `assembleDebugAndroidTest` 通过；`lintDebug` 0 错误 0 警告。

## [1.0.1] - 2026-09-24

### 新增
- **打卡两种模式（每个习惯可单独选择）**：
  - **周期模式**（原）：以创建日为锚点的固定周期日，每周期最多打卡 1 次；
  - **自由模式**（新）：可随时打卡，打卡后 N 天内再打即连续，超过 N 天未打卡记一次缺卡，下次打卡重新起算；
  - 两模式各自的月份视图、连续/最长/累计统计与缺卡（断签）提示均已适配；数据表 `habits` 新增 `mode` 列（Room v3 迁移，旧习惯默认周期模式）。
- **日程 → 周视图改为「节次课表」（仿 BIT101）**：每天按 13 节划分（08:00–08:45 第1节 … 20:10–20:55 第13节，时间与 BIT101 默认值一致），带节次轴与横线分界；日程块按起止时间吸附到节次行；
- **设置 → 日程设置**：显示周六 / 显示周日 / 高亮今日 / 显示节次分界线 / 显示边框（开关即时生效并记住）；
- **导入 BIT101 课表**：设置 → 日程设置 → 导入课表（JSON）——粘贴与 BIT101 课程对象同构的 JSON（name/teacher/classroom/weekday/start_section/end_section/weeks），设置学期开始日期，预览后批量转为周期日程（含教师·周次备注）；
- 数据模型：`schedules` 表新增 `note` 列（BIT101 导入备注）；Room v1→v2→v3 迁移，已有数据无损升级。

### 验证
- JVM 单元测试 54 例全部通过（新增节次表 3 例、课表 JSON 导入 5 例、打卡时间线 8 例）；
- `assembleDebug` / `assembleDebugAndroidTest` 通过；`lintDebug` 0 错误 0 警告。

## [1.0.0] - 2026-09-24（首个可用版本）

### 功能
- **倒数日**：新增/编辑（标题、目标日期、备注）；显示「还有 N 天 / 就是今天 / 已过 N 天」；可「置顶桌面」。
- **习惯打卡**：每日 / 每周 / 自定义每 N 天；每周期最多打卡 1 次；月历打卡视图（已打卡 / 断签 / 今日 / 未来）；
  连续打卡统计（当前 / 最长 / 累计 / 断签次数）；今日待打卡横幅。
- **日程**：周视图（课表式，6:00–23:00 时间网格，点击空白时段快速新建）+ 月视图（月历角标 + 当日议程）；
  **单次日程**与**批量周期日程**（日期范围 × 星期多选 × 时间段，如「3月1日~6月30日之间每个周一上午 08:00~09:50」）。
- **设置**：底部导航 日程 / 倒数 / 打卡 / 设置；主题切换（跟随系统 / 亮色 / 暗色）；关于（版本信息）；
  **应用内检查更新**（读取 GitHub 最新 Release，有新版本时下载 APK 并调用系统安装器）。
- **桌面小组件**：倒数日 2×2 / 4×1 双布局；置顶优先展示；点击打开 App；每日刷新 +
  开机 / 日期 / 时间 / 时区变化自动刷新（iQOO/vivo 的 goAsync 兼容由 Glance 自动处理）。

### 数据与隐私
- 全部数据保存在本机 Room（SQLite），核心功能完全离线；
- `INTERNET` 权限仅用于「检查更新」；`REQUEST_INSTALL_PACKAGES` 仅用于安装更新包。

### 验证
- JVM 单元测试 38 例全部通过（日期 / 周期展开 / 连续打卡 / 倒数 / 日程物化 / 版本比较 / Release JSON 解析）；
- `assembleDebug` / `assembleRelease` / `assembleDebugAndroidTest` 构建通过；`lintDebug` 0 错误 0 警告；
- Room 数据层集成测试 3 例位于 `app/src/androidTest`（真机 / 模拟器运行）。