# 更新日志

本项目的发布节奏与「应用内检查更新」绑定：每个版本以 `vX.Y.Z` 标签发布到
[GitHub Releases](https://github.com/lmhy006/studymate-android/releases)，并附带 APK 资产。
发布步骤见 [docs/RELEASE.md](docs/RELEASE.md)。

## [1.0.1] - 2026-09-24

### 新增
- **应用内更新双源自动切换**：GitHub 优先，失败自动切到 Gitee（码云）备用源，下载 APK 失败也会回退到 Gitee（国内直连友好；需把仓库镜像到 Gitee 并同步发布同名 Release）；
- **逐课速录课表**：表单式录入 课程名/星期/起止节次/教学周/教师/教室，可连续添加多门后生成导入（与 JSON 导入同构，免手写 JSON）；
- **打卡模式选择优化**：仅在选择「每 N 天」时才显示 周期/自由 模式选择；每日、每周固定为周期模式；
- **打卡支持两种模式（每个习惯可单独选择）**：
  - **周期模式**（原）：以创建日为锚点的固定周期日，每周期最多打卡 1 次；
  - **自由模式**（新）：可随时打卡，打卡后 N 天内再打即连续，超过 N 天未打卡记一次缺卡，下次打卡重新起算；
  - 两模式各自的月份视图、连续/最长/累计统计与缺卡（断签）提示均已适配；数据表 `habits` 新增 `mode` 列（Room v3 迁移，旧习惯默认周期模式）。
- **日程 → 周视图改为「节次课表」（仿 BIT101）**：每天按 13 节划分（08:00–08:45 第1节 … 20:10–20:55 第13节，时间与 BIT101 默认值一致），带节次轴与横线分界；日程块按起止时间吸附到节次行；
- **设置 → 日程设置**：显示周六 / 显示周日 / 高亮今日 / 显示节次分界线 / 显示边框（开关即时生效并记住）；
- **导入 BIT101 课表**：设置 → 日程设置 → 导入课表（JSON）——粘贴与 BIT101 课程对象同构的 JSON（name/teacher/classroom/weekday/start_section/end_section/weeks），设置学期开始日期，预览后批量转为周期日程（含教师·周次备注）；
- 数据模型：`schedules` 表新增 `note` 列（BIT101 导入备注）；Room v1→v2→v3 迁移，已有数据无损升级。

### 验证
- JVM 单元测试 54 例全部通过（新增节次表 3 例、课表 JSON 导入 5 例、打卡时间线 8 例）；
- `assembleDebug` / `assembleDebugAndroidTest` 通过；`lintDebug` 0 错误 0 警告。

## [1.0.0] - 2026-09-24（首个可用版本）

### 功能
- **倒数日**：新增/编辑（标题、目标日期、备注）；显示「还有 N 天 / 就是今天 / 已过 N 天」；可「置顶桌面」。
- **习惯打卡**：每日 / 每周 / 自定义每 N 天；每周期最多打卡 1 次；月历打卡视图（已打卡 / 断签 / 今日 / 未来）；
  连续打卡统计（当前 / 最长 / 累计 / 断签次数）；今日待打卡横幅。
- **日程**：周视图（课表式，6:00–23:00 时间网格，点击空白时段快速新建）+ 月视图（月历角标 + 当日议程）；
  **单次日程**与**批量周期日程**（日期范围 × 星期多选 × 时间段，如「3月1日~6月30日之间每个周一上午 08:00~09:50」）。
- **设置**：底部导航 日程 / 倒数 / 打卡 / 设置；主题切换（跟随系统 / 亮色 / 暗色）；关于（版本信息）；
  **应用内检查更新**（读取 GitHub 最新 Release，有新版本时下载 APK 并调用系统安装器）。
- **桌面小组件**：倒数日 2×2 / 4×1 双布局；置顶优先展示；点击打开 App；每日刷新 +
  开机 / 日期 / 时间 / 时区变化自动刷新（iQOO/vivo 的 goAsync 兼容由 Glance 自动处理）。

### 数据与隐私
- 全部数据保存在本机 Room（SQLite），核心功能完全离线；
- `INTERNET` 权限仅用于「检查更新」；`REQUEST_INSTALL_PACKAGES` 仅用于安装更新包。

### 验证
- JVM 单元测试 38 例全部通过（日期 / 周期展开 / 连续打卡 / 倒数 / 日程物化 / 版本比较 / Release JSON 解析）；
- `assembleDebug` / `assembleRelease` / `assembleDebugAndroidTest` 构建通过；`lintDebug` 0 错误 0 警告；
- Room 数据层集成测试 3 例位于 `app/src/androidTest`（真机 / 模拟器运行）。